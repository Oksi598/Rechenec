# Порожній VITE_API_BASE: усі /api та /hubs йдуть на Vite, далі — проксі на Kestrel.
VITE_API_BASE=

# Адреса бекенду для проксі (має збігатися з dotnet run — порт 5212 у профілі http / https).
VITE_API_PROXY=http://127.0.0.1:5212
